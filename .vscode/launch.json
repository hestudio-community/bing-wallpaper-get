{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run API",
            "request": "launch",
            "command": "npm run server",
            "cwd": "${workspaceFolder}"
        }
    ]
}